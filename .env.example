# Copy this file to ".evn" (the spelling this project already uses) and fill in
# real values. Never commit the filled-in file — .gitignore excludes it.

# --- Supabase ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# --- Analytics / ads (optional) ---
NEXT_PUBLIC_GA_MEASUREMENT_ID=
NEXT_PUBLIC_ADSENSE_ID=

# --- Payments (optional, for the Give page) ---
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# --- AI provider keys (optional, server-side only) ---
OPENAI_API_KEY=
GEMINI_API_KEY=
ANTHROPIC_API_KEY=
PERPLEXITY_API_KEY=
